1. define a combo that is used if all models in a given combo failed
2. can I know which is the client/tool that is calling this service? Can I have tool-based combos?
3. is there a tool that chooses models by weights (similar to ccr)?
